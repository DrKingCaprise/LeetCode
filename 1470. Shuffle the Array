int* shuffle(int* nums, int numsSize, int n, int* returnSize){
    int* ans = malloc(sizeof(int) * numsSize);
    int count = 0;

    for(int i = 0; i < n; i++){
        ans[count++] = nums[i];
        ans[count++] = nums[n + i];
    }
    *returnSize = numsSize;
    return ans;
}
