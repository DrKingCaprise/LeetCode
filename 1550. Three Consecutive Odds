bool threeConsecutiveOdds(int* arr, int arrSize) {
    
    int count = 0;

    for(int i = 0; i < arrSize; i++){
        (arr[i] % 2 == 0) ? (count = 0) : count++;
        if (count == 3)
            return true;
    }
    return false;
}
